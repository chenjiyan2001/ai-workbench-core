# Git 操作指南

本文档提供常见 Git 操作场景的详细指南。基础规范见 [git.md](git.md)，Claude 行为规范见 [git-claude.md](git-claude.md)。

---

## 常用命令速查

### 🟢 安全操作 (只读)

```bash
git status                    # 查看状态
git diff                      # 查看改动
git diff --staged             # 查看已暂存改动
git log --oneline -10         # 查看最近10条提交
git branch                    # 查看本地分支
git branch -a                 # 查看所有分支
git fetch                     # 获取远程更新
git reflog                    # 查看操作历史 (后悔药)
```

### 🟡 谨慎操作 (有副作用但可恢复)

```bash
git add <file>                # 暂存指定文件
git add .                     # 暂存所有改动
git checkout -b <branch>      # 创建并切换分支
git switch -c <branch>        # 创建并切换分支 (新语法)
git stash push -u -m "msg"    # 暂存当前工作
git stash pop                 # 恢复暂存的工作
git pull --ff-only            # 安全拉取 (仅快进)
git restore <file>            # 丢弃工作区改动
git restore --staged <file>   # 取消暂存
```

### 🔴 危险操作 (需要确认)

```bash
git commit -m "message"       # 提交
git push                      # 推送到远程
git merge <branch>            # 合并分支
git branch -d <branch>        # 删除已合并分支
git reset --soft HEAD~1       # 撤销提交 (保留改动)
git cherry-pick <sha>         # 挑拣提交
```

### ⛔ 禁止操作 (高风险)

```bash
git push --force              # 强制推送
git reset --hard              # 硬重置 (丢失改动)
git clean -fd                 # 删除未跟踪文件
git rebase                    # 变基操作
```

---

## 场景1: 撤销与回退

### 典型触发条件

```
用户可能说:
├── "刚刚提交错了"
├── "commit message 写错了"
├── "把敏感信息提交进去了"
├── "需要回到某个稳定版本"
└── "这个提交不该进来，想撤销"
```

### 操作决策树

```
要撤销什么?
├── 工作区改动 (未 add):
│   └── git restore <file>                    # 丢弃单文件改动
│
├── 暂存区改动 (已 add 未 commit):
│   └── git restore --staged <file>           # 取消暂存，保留改动
│
├── 本地提交 (未 push):
│   ├── 只改提交信息 → git commit --amend
│   ├── 撤销但保留改动 → git reset --soft HEAD~1
│   └── 完全丢弃 → git reset --hard HEAD~1 (危险!)
│
└── 已推送提交:
    ├── 共享分支 → git revert <sha>           # 创建反向提交
    └── 个人分支 → git reset + push --force-with-lease (危险!)
```

### git restore 使用方法 (推荐)

```bash
# 丢弃工作区单文件改动
git restore path/to/file.py

# 丢弃工作区所有改动
git restore .

# 取消暂存 (保留工作区改动)
git restore --staged path/to/file.py

# 同时丢弃暂存和工作区改动
git restore --staged --worktree path/to/file.py

# 恢复到特定版本
git restore --source=HEAD~3 path/to/file.py
```

### git reflog 使用方法 (后悔药)

```bash
# 查看所有操作历史
git reflog

# 输出示例:
# a1b2c3d HEAD@{0}: reset: moving to HEAD~1
# e4f5g6h HEAD@{1}: commit: feat: 添加功能
# i7j8k9l HEAD@{2}: checkout: moving from main to feature

# 恢复到误删除的提交
git reset --hard HEAD@{1}

# 或创建新分支保存
git branch recovery-branch HEAD@{1}
```

### ⚠️ 敏感信息泄露处理

```
IF 用户说"提交了密码/密钥/token":
    1. 立即停止，不要继续推送
    2. git revert 只能撤销内容，无法从历史中抹除
    3. 建议: "请立即轮换泄露的密钥"
    4. 如需彻底清除历史，使用 git filter-branch 或 BFG (需专业操作)
```

---

## 场景2: 分支合并

### 典型触发条件

```
用户可能说:
├── "功能开发好了要合并"
├── "PR 冲突了，帮我解决"
├── "把主干最新改动合到我的分支"
└── "需要保持线性历史"
```

### 操作决策树

```
合并方向?
├── 主干 → 功能分支 (同步更新):
│   └── git fetch && git merge origin/main
│
└── 功能分支 → 主干 (完成合并):
    └── 推荐: 通过 PR/MR 合并
```

### 冲突处理流程

```bash
# 1. 查看冲突文件
git status

# 2. 编辑解决冲突
# 冲突标记:
# <<<<<<< HEAD
# 当前分支代码
# =======
# 合并分支代码
# >>>>>>> branch-name

# 3. 标记已解决
git add <冲突文件>

# 4. 继续合并
git merge --continue

# 或放弃合并
git merge --abort
```

---

## 场景3: 挑拣提交 (Cherry-pick)

### 典型触发条件

```
用户可能说:
├── "把某个提交单独拿到这个分支"
├── "只需要那个 bug 修复，不要整个分支"
├── "hotfix 需要同时合到 main 和 release"
└── "那个功能的某个提交我想用"
```

### 基本操作

```bash
# 挑拣单个提交
git cherry-pick <sha>

# 挑拣多个提交
git cherry-pick <sha1> <sha2> <sha3>

# 挑拣一个范围 (不包含起点)
git cherry-pick <start-sha>..<end-sha>

# 挑拣一个范围 (包含起点)
git cherry-pick <start-sha>^..<end-sha>
```

### 选项说明

```bash
# 只应用改动，不自动提交 (可以修改后再提交)
git cherry-pick --no-commit <sha>

# 在提交信息中记录原始提交
git cherry-pick -x <sha>
# 结果: (cherry picked from commit abc123...)

# 保留原作者信息
git cherry-pick --signoff <sha>
```

### 冲突处理

```bash
# 解决冲突后继续
git cherry-pick --continue

# 跳过当前提交
git cherry-pick --skip

# 放弃整个 cherry-pick
git cherry-pick --abort
```

### 典型场景: Hotfix 同步

```bash
# 场景: hotfix 分支修复了 bug，需要同步到 main

# 1. 切到 main
git checkout main

# 2. 挑拣 hotfix 的修复提交
git cherry-pick <hotfix-commit-sha>

# 3. 推送
git push
```

---

## 场景4: Hotfix 紧急修复流程

### 典型触发条件

```
用户可能说:
├── "生产环境有个紧急 bug"
├── "线上出问题了，需要马上修"
├── "发布的版本有个严重问题"
└── "需要发一个补丁版本"
```

### 完整 Hotfix 流程

```bash
# 1. 从生产版本 tag 创建 hotfix 分支
git fetch --tags
git checkout -b hotfix/critical-bug v1.2.0

# 2. 修复 bug
# ... 编辑文件 ...

# 3. 提交修复
git add .
git commit -m "fix: 修复生产环境xxx问题"

# 4. 打补丁版本 tag
git tag -a v1.2.1 -m "hotfix: 修复xxx问题"

# 5. 推送 hotfix 分支和 tag
git push -u origin hotfix/critical-bug
git push origin v1.2.1

# 6. 部署 v1.2.1 到生产环境

# 7. 将修复同步到 main (通过 PR 或 cherry-pick)
git checkout main
git cherry-pick <hotfix-commit-sha>
git push

# 8. 删除 hotfix 分支
git branch -d hotfix/critical-bug
git push origin --delete hotfix/critical-bug
```

### Hotfix 决策树

```
紧急程度?
├── 极紧急 (分钟级):
│   └── 从 tag 切 hotfix → 修复 → 打 tag → 部署 → 再同步 main
│
└── 一般紧急 (小时级):
    └── 从 main 切 fix 分支 → 修复 → PR → 合并 → 打 tag → 部署
```

---

## 场景5: 暂存与恢复 (Stash)

### 典型触发条件

```
用户可能说:
├── "我得切到别的分支修 bug，但现在改了一半"
├── "先把当前工作保存起来"
├── "拉取前提示有本地修改冲突"
└── "恢复之前保存的工作"
```

### 操作流程

```bash
# 保存当前工作 (推荐带 -u 包含未跟踪文件)
git stash push -u -m "wip: 功能描述"

# 查看暂存列表
git stash list

# 查看暂存内容
git stash show -p stash@{0}

# 恢复工作 (推荐先 apply 再 drop)
git stash apply stash@{0}   # 应用但保留记录
git stash drop stash@{0}    # 确认无误后删除

# 或一步完成 (有风险)
git stash pop               # 应用并删除
```

### ⚠️ 注意事项

- `stash pop` 冲突后记录已删除，建议先 `apply` 确认无误再 `drop`
- 忘记 `-u` 会导致未跟踪文件丢失
- 重要进度推荐 WIP 提交而非 stash

---

## 场景6: 同步与更新

### 典型触发条件

```
用户可能说:
├── "我本地落后了/需要同步最新代码"
├── "pull 的时候提示 diverged"
├── "push 被拒绝了"
└── "需要跟主干对齐"
```

### 安全同步流程

```bash
# 1. 获取远程更新 (清理已删除的远程分支)
git fetch --prune

# 2. 查看本地与远程差异
git log --oneline HEAD...origin/main

# 3. 安全更新 (仅快进合并)
git pull --ff-only

# 如果失败 (出现分叉)，使用 merge
git merge origin/main
```

### push 被拒绝处理

```
push 被拒 (non-fast-forward)?
├── 原因: 本地落后于远程
├── 解决:
│   ├── git fetch
│   ├── git merge origin/<branch>
│   ├── 解决冲突 (如有)
│   └── git push
└── 禁止: git push --force (除非确认是个人分支且已备份)
```

---

## 场景7: 查看与对比

### 典型触发条件

```
用户可能说:
├── "这个改动是谁改的/什么时候改的"
├── "对比两个版本差异"
├── "查看某个文件的历史"
├── "找出引入 bug 的提交"
└── "看看这次改动会影响哪些文件"
```

### 常用查看命令

```bash
# 查看提交历史
git log --oneline --graph -20

# 查看某文件历史
git log --oneline -- <file>
git log -p -- <file>           # 包含改动内容

# 对比差异
git diff                       # 工作区 vs 暂存区
git diff --staged              # 暂存区 vs HEAD
git diff main..feature/xxx     # 两个分支差异
git diff HEAD~3..HEAD          # 最近 3 次提交差异

# 追溯某行代码
git blame <file>
git blame -w <file>            # 忽略空白变化

# 查找引入 bug 的提交 (二分查找)
git bisect start
git bisect bad                 # 当前版本有 bug
git bisect good <sha>          # 某个好的版本
# ... 测试并标记 good/bad ...
git bisect reset               # 结束
```

---

## 场景8: 清理与维护

### 典型触发条件

```
用户可能说:
├── "这个分支合并完了想删掉"
├── "本地有一堆没用的分支"
├── "工作区很脏，想清理"
└── "误生成了很多临时文件"
```

### 分支清理

```bash
# 查看已合并的分支
git branch --merged main

# 删除本地分支 (已合并)
git branch -d feature/xxx

# 强制删除本地分支 (未合并，谨慎!)
git branch -D feature/xxx

# 删除远程分支
git push origin --delete feature/xxx

# 清理远程追踪引用
git fetch --prune
```

### 工作区清理

```bash
# ⚠️ 危险操作！先预览
git clean -n                   # 预览将删除的文件
git clean -nd                  # 预览将删除的文件和目录

# 确认后执行 (谨慎!)
git clean -f                   # 删除未跟踪文件
git clean -fd                  # 删除未跟踪文件和目录

# 极危险！会删除 .gitignore 中的文件
git clean -fdx                 # 禁止使用
```

---

## 推荐 Git 配置

```bash
# 安全默认配置
git config --global pull.ff only           # pull 仅快进，避免意外 merge
git config --global fetch.prune true       # fetch 自动清理远程删除的分支
git config --global push.default simple    # push 只推当前分支

# 实用配置
git config --global core.autocrlf input    # Linux/Mac
git config --global core.autocrlf true     # Windows
git config --global init.defaultBranch main

# 别名 (可选)
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.lg "log --oneline --graph -20"
```
